# FastAPI and web framework
# NOTE: requires Python 3.10+ (MCP SDK). Versions below are the verified set
# that runs the app + the MCP server together.
fastapi==0.135.1
starlette==0.52.1
uvicorn[standard]==0.41.0
python-multipart>=0.0.9  # mcp 要求 >=0.0.9

# Database
sqlmodel==0.0.37
alembic==1.13.1
pymysql==1.1.0
cryptography==41.0.7

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<5.0.0

# Validation
pydantic==2.12.5
pydantic-settings==2.13.1

# MCP server (Streamable HTTP). Requires Python 3.10+.
mcp==1.27.2

# CORS
fastapi-cors==0.0.6

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.28.1
requests>=2.31.0

# Object Storage
tos>=2.7.0

# Export
openpyxl>=3.1.0
pandas>=2.0.0
python-docx>=1.1.0

# Knowledge Base
chromadb>=0.4.24
PyPDF2>=3.0.0
