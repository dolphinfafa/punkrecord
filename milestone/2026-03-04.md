# 2026-03-04 项目进度记录

## 今日完成

1. 修复项目管理“创建项目”弹窗无法下滑问题
- 现象：创建项目时，弹窗底部字段在小屏或内容较多时无法滚动到可点击区域。
- 处理：为项目创建与编辑弹窗的 `form` 增加纵向 `flex` 布局约束（`display: flex; flex-direction: column; flex: 1; min-height: 0`），确保 `.modal-body` 的滚动容器生效。
- 结果：表单可正常向下滚动，底部操作区可访问。

2. 完成问题根因追溯（引入该问题的改动）
- 通过 `git blame` / `git show` 定位到提交：
  - `8c32a1fe8ac9e952f6d4527842edd7f2f8fd83b4`
  - `feat: complete project management enhancements`
  - 时间：`2026-02-20 22:43:09 +0800`
- 结论：该提交新增了项目创建/编辑弹窗结构（`.modal-content > form > .modal-body`），但 `form` 未设置为可承载滚动布局的 `flex` 容器，在 `modal-content` 的 `max-height + overflow: hidden` 组合下触发内容裁切。

3. 补充工程级“防回归约定”（不仅限样式）
- 在 `.agent/workflows/project-index.md` 新增 `Section 11: Change Safety and Regression Guardrails`，覆盖：
  - 模块边界与作用域约束
  - API/Schema 契约兼容
  - 变更风险分级
  - 单测/集成/E2E 回归要求
  - PR/评审门禁
  - 数据迁移安全
  - 灰度发布与回滚
  - 监控与复盘闭环

4. 合并 Python 环境规范并清理重复文档
- 已将 `python-environment.md` 内容并入 `project-index.md` 的 `Python environment policy`。
- 已删除 `.agent/workflows/python-environment.md`，并移除对应引用，避免规范分散。

## 影响文件

- `frontend/src/pages/project/components/ProjectCreateModal.jsx`
- `frontend/src/pages/project/components/ProjectEditModal.jsx`
- `.agent/workflows/project-index.md`
- `.agent/workflows/python-environment.md`（删除）

## 验证

- 已通过代码级检查确认弹窗滚动容器结构修复到位。
- 已通过 `git blame` / `git show` 完成问题引入提交与改动点定位。
- 待补充：前端页面手工联调（项目管理创建/编辑弹窗在不同窗口高度下滚动与提交流程）。

---

## 今日增量更新（待办与 Bug 管理）

5. 开发进度与任务管理能力增强
- 开发进度页取消首次自动同步功能清单，改为手动触发同步后再创建任务。
- 新增任务一键清空（级联删除关联待办）。
- 新增批量指定负责人。
- 新增单任务删除（级联删除关联待办）。
- 移除开发进度页“刷新”按钮。

6. Bug 管理能力增强
- Bug 新建支持多图上传（`bug_images`），并兼容旧字段 `bug_image`。
- Bug 列表支持点开单条详情，详情中补全并展示完整字段：
  - 人员（开发/测试/审核/创建）
  - 时间（开始/截止/完成/创建/更新）
  - 结果与步骤（实际结果/期望结果/复现步骤）
  - 原因与备注（阻塞/审核/忽略/备注）
  - 标签与扩展字段（`link` 未知键值自动展示）
- Bug 列表页不再展示附件缩略图，附件仅在详情弹窗中展示。
- 修复“新建 Bug 出现两条重复待办”：移除镜像待办重复创建逻辑，仅保留单条 Bug 任务。

7. 待办事项增强
- 新建任务支持：
  - 可选所属项目（不选则保持自定义任务）
  - 多图上传
- 任务详情支持图片多张上传、删除、放大缩小预览。
- 修复任务图片点击无法打开问题（下载 API 返回结构与前端解析兼容）。
- 团队任务详情支持编辑（直属主管可编辑下属任务）。
- 新增筛选：按任务类型、负责人筛选。
- 看板卡片不展示上传图片，展示负责人信息。

8. 后端与契约修复
- `todo` 增加图片接口：上传/下载/删除。
- `project` 的任务响应补充 `link` 等字段，确保 Bug 详情可完整渲染。

9. 组件抽象与文档同步
- 提炼通用组件：
  - `frontend/src/components/common/BugImagePreview.jsx`
  - `frontend/src/components/common/TodoImagePreview.jsx`
  - `frontend/src/components/common/BugDetailModal.jsx`
- 已同步更新 `.agent/workflows/project-index.md`（API、组件、行为约定）。

## 本次增量影响文件（补充）

- `backend/app/api/todo.py`
- `backend/app/schemas/project.py`
- `frontend/src/api/project.js`
- `frontend/src/api/todo.js`
- `frontend/src/pages/project/DevelopmentProgressPage.jsx`
- `frontend/src/pages/project/DevelopmentProgressPage.css`
- `frontend/src/pages/project/components/BugManagementModal.jsx`
- `frontend/src/pages/todo/TodoPage.jsx`
- `frontend/src/pages/todo/TodoPage.css`
- `frontend/src/components/todo/TodoModal.jsx`
- `frontend/src/components/todo/TodoModal.css`
- `frontend/src/components/todo/TodoDetailModal.jsx`
- `frontend/src/components/todo/TodoDetailModal.css`
- `frontend/src/components/common/BugImagePreview.jsx`
- `frontend/src/components/common/BugImagePreview.css`
- `frontend/src/components/common/TodoImagePreview.jsx`
- `frontend/src/components/common/TodoImagePreview.css`
- `frontend/src/components/common/BugDetailModal.jsx`
- `frontend/src/components/common/BugDetailModal.css`
- `.agent/workflows/project-index.md`
