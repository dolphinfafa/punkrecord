# 2026-03-01 项目进度记录

## 今日完成

1. 后端数据库兼容修复迁移改造
- 将启动阶段的数据库兼容 SQL 修复流程从运行时内联执行，迁移为 Alembic 版本化迁移。
- `backend/app/core/database.py` 已重构：
  - 移除 `_ensure_legacy_columns()` 启动期直接回填逻辑；
  - 新增 `run_migrations()`，在 `create_db_and_tables()` 后执行 `alembic upgrade head`。

2. 新增迁移基础设施
- 新增 Alembic 配置文件：`backend/alembic.ini`。
- 新增迁移目录：`backend/db_migrations/`。
- 新增迁移环境文件：`backend/db_migrations/env.py`。
- 新增迁移脚本：`backend/db_migrations/versions/20260301_0001_legacy_compatibility_fixes.py`。

3. 兼容迁移脚本覆盖范围
- `user`：补列并回填 `beili_balance`、`leave_balance_reset_year`、各类假期余额字段。
- `beli_rule`：补 `rule_type` 并修复空值。
- `project` / `project_stage`：补 `attachments` 并回填空值为 `[]`。
- `finance_transaction`：补 `txn_type`、`employee_user_id`，并规范化 `txn_type` / `reconcile_status` 枚举值。

4. 工程索引同步
- 已同步更新 `.agent/workflows/project-index.md`：
  - 目录结构由 `backend/migrations` 更新为 `backend/db_migrations`；
  - 追加 2026-03-01 增量更新记录。

5. 验收报告生成功能
- **后端 API**：在 `backend/app/api/project.py` 及新服务 `backend/app/services/docx_generator.py` 中实现了 `GET /projects/{project_id}/acceptance-report/download` 接口，基于 `python-docx` 根据项目的需求字段动态生成并排版带有签名栏配置和预置表格颜色的验收测试报告（.docx）。
- **前端页面**：在 `ProjectDetailPage.jsx` 的交付页面阶段栏增加“验收报告”按钮，并在 `projectApi.js` 增加下载流调用的 `downloadAcceptanceReport`。

## 验证与说明

- 语法级检查：迁移相关 Python 文件已完成编译检查（通过）。
- 运行级验证：当前环境缺少 `alembic`/`sqlmodel` 依赖，未完成真实迁移执行验证。
- 待环境就绪后建议执行：
  - `conda activate punkrecord`
  - `pip install -r backend/requirements.txt`
  - `cd backend && python -m alembic -c alembic.ini upgrade head`

## 风险与后续

- 启动阶段现在依赖 Alembic；若运行环境未安装依赖，启动会报错并中止。
- 后续建议补一条开发文档说明：首次拉起需先安装依赖并执行迁移。
