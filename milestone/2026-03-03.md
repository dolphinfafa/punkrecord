# 2026-03-03 项目进度记录

## 今日完成

1. 小程序 Todo 模块补齐“我的/团队”双视图
- `pages/todo/index.*` 新增“我的待办 / 团队待办”切换。
- 团队待办支持按状态筛选、关键词搜索，并展示负责人信息。
- 对待审核任务补齐团队侧“通过/驳回”动作入口（按权限显示）。

2. 小程序请假申请与审批流程接入
- 在 `pages/todo/index.*` 内新增请假申请区块：
  - 请假类型选择、起止日期、原因输入、提交。
  - 我的请假记录展示（类型/状态/时间范围）。
  - 待我审批列表展示（通过/驳回）。
- `services/todo.js` 新增请假相关接口封装：
  - `createLeave` / `listMyLeaves` / `listTeamPendingLeaves` / `approveLeave` / `rejectLeave`。

3. 底部导航改为自定义 TabBar（样式与可读性优化）
- 新增 `miniprogram/custom-tab-bar/` 组件（`index.js/json/wxml/wxss`）。
- `app.json` 切换 `tabBar.custom = true`。
- 底部菜单文本字号提升并增加图标字符，改善可读性。
- `app.wxss` 增加容器底部安全区 padding，避免页面内容被导航遮挡。

4. 财务账户“未命名账户”问题修复
- 排查结论：前端字段映射问题，不是 API 调用失败。
- `pages/finance/index.js` 将账户名称优先字段从 `name` 修正为 `account_name`。
- 账户列表补充副信息显示（银行名或账户类别），并微调列表样式。

5. 登录后“我的”页显示访客问题修复
- 根因：登录后先请求 `/auth/me` 再写 token，导致用户信息获取失败并落库为 `null`。
- 修复：`pages/login/index.js` 先写 token，再请求 `/auth/me`。
- 兜底：`pages/mine/index.js` 在本地 user 为空但 token 存在时自动回拉 `/auth/me` 并缓存。

## 影响文件

- `miniprogram/app.json`
- `miniprogram/app.wxss`
- `miniprogram/custom-tab-bar/*`
- `miniprogram/services/todo.js`
- `miniprogram/pages/todo/index.js`
- `miniprogram/pages/todo/index.wxml`
- `miniprogram/pages/todo/index.wxss`
- `miniprogram/pages/finance/index.js`
- `miniprogram/pages/finance/index.wxml`
- `miniprogram/pages/finance/index.wxss`
- `miniprogram/pages/login/index.js`
- `miniprogram/pages/mine/index.js`
- `.agent/workflows/project-index.md`

## 验证

- 已通过 `node --check` 对改动涉及的 JS 文件做语法检查。
- 待在微信开发者工具进行页面级联调（Todo/请假/TabBar/我的页登录态）。

## 增量更新（2026-03-03：项目板块完善）

1. 项目列表与详情页能力补齐
- `pages/project/index.*` 增强为“统计 + 状态筛选 + 关键词搜索 + 进度条 + 详情跳转”。
- 新增 `pages/project/detail/*`，按“概览/阶段/成员/任务”分区展示项目执行信息。

2. 阶段展示与操作命名对齐 Web 端
- 阶段行内移除“交付物”显示，保留状态、计划时间、附件数与操作入口。
- 阶段操作文案改为与 Web 一致：
  - `功能清单`、`报价单`、`AI生成合同`、`原型确认单`、`开发进度`、`Bug管理`、`验收报告`。

3. 阶段附件入口结构调整
- 调整为“阶段页签统一附件入口”模式：集中展示全部阶段附件并可下载。
- 每个阶段行仅显示附件数量，不再放单独附件下载链接。

4. 项目成员小程序端可编辑
- 在项目详情“成员”页签支持成员添加/移除。
- 仅项目 `PM/Owner` 显示编辑能力，且保护 PM/Owner 不可被移除。

5. 服务层与路由同步
- `miniprogram/services/project.js` 补齐项目详情相关接口（详情/阶段/成员/任务/成员增删）。
- `miniprogram/app.json` 注册 `pages/project/detail/index` 页面路由。

6. 冗余性核查结论（阶段附件）
- 后端 `project_stage.attachments` 并非孤立残留，已有模型字段、迁移与上传/下载/删除接口闭环。
- 当前阶段不执行数据库清理；先保留后端能力，仅在小程序侧调整入口与展示结构。
