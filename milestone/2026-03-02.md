# 2026-03-02 项目进度记录

## 今日完成

1. workflows 文档跨平台乱码问题治理
- 对 `.agent/workflows` 下全部工作流文档进行统一整理与重写。
- 文档编码统一为 UTF-8（无 BOM），行尾统一为 LF，避免 Windows/macOS 间显示差异。
- 新增仓库级编码/换行约束：`.editorconfig`、`.gitattributes`。

2. workflows 内容英文化重构
- 将 `workflows` 目录内文档改写为英文版本，减少系统区域设置/默认编码带来的风险。
- 重点重写文件：
  - `project-index.md`
  - `use-project-index.md`
  - `python-environment.md`
- 其他 workflow 文件同步改为英文说明，保留文件职责与用途。

3. 工程索引（project-index）完整化
- 在英文版基础上补齐完整导航信息，供后续 Agent 快速理解工程：
  - 仓库目录结构与模块分层
  - 后端路由架构与关键 API 面
  - 核心数据模型分组
  - 前端路由与页面映射
  - 关键业务流程、运行命令、约定与维护规则
- 更新时间更新为 `2026-03-02`。

## 验证与说明

- 已在本地进行文本级检查：`project-index.md` 当前为纯英文内容，无乱码字符。
- `file -I` 检查显示 workflows 文件编码稳定（ASCII/UTF-8 兼容）。
- `rg -n "\\r$"` 未检出 CRLF 行尾。

## 风险与后续

- 若 GitHub 页面仍显示旧乱码，通常为远端分支未同步最新提交；推送后应恢复正常。
- 后续新增/修改 workflow 文档时，保持英文 + UTF-8/LF 规范，避免再次引入平台相关编码问题。

## 增量更新（2026-03-02：微信小程序端启动）

1. 新建微信小程序工程骨架（`miniprogram/`）
- 初始化 `app.js`、`app.json`、`app.wxss`、`sitemap.json`、`project.config.json`。
- 配置 5-tab 移动导航：Home / Todo / Project / Finance / Mine。
- 统一基础样式为移动端卡片式布局，使用 `rpx` 适配手机屏幕。

2. 新增小程序端基础能力
- 登录与会话管理：
  - `pages/login/index.*`
  - `utils/storage.js`（token/user 持久化）
- 请求封装：
  - `utils/request.js`
  - 统一附带 Bearer Token，处理 401 自动清理会话并回登录页。

3. 迁移 Web 端核心模块到小程序首版页面
- 首页聚合：`pages/home/index.*`（Todo/Project/Finance/Leave 汇总 + 快捷入口）
- 待办列表：`pages/todo/index.*`（对接 `/api/v1/todo/my`）
- 项目列表：`pages/project/index.*`（对接 `/api/v1/project/projects`）
- 财务概览：`pages/finance/index.*`（对接账户与交易列表）
- 我的页面：`pages/mine/index.*`（个人信息 + 退出登录）
- 子页入口：
  - 合同：`pages/contract/index.*`
  - IAM 用户：`pages/iam/index.*`

4. 新增小程序 API 服务映射
- `services/auth.js`
- `services/todo.js`
- `services/project.js`
- `services/finance.js`
- `services/contract.js`
- `services/iam.js`

5. 工程索引同步
- 已更新 `.agent/workflows/project-index.md`：
  - 目录结构新增 `miniprogram/`。
  - 前端架构补充小程序端模块说明。
  - 运行说明补充微信开发者工具导入方式。
  - 约定补充小程序页面命名与移动端样式规范。

## 当前状态与下一步

- 当前为“可运行首版”，已具备登录、鉴权、列表展示与模块导航。
- 下一阶段建议按 Web 业务逐模块补全：
  1) Todo 状态流转与请假审批操作；
  2) Project 详情/阶段/成员/任务排期；
  3) Finance 交易创建与报销提交；
  4) 合同详情与审批动作；
  5) IAM 管理类编辑与权限控制。

## 增量更新（2026-03-02：小程序编译基础库修复）

1. 问题定位
- 微信开发者工具编译报错：下载基础库 `2.31.0` 失败，版本不存在或已下架。
- 根因在本地私有配置 `miniprogram/project.private.config.json`，其 `libVersion` 被固定为 `2.31.0`。

2. 修复动作
- 将 `project.private.config.json` 中的 `libVersion` 更新为 `3.6.3`（与项目公开配置保持一致）。
- 建议在开发者工具执行“清缓存 -> 清除全部缓存”后重新编译。

3. 索引同步
- 已在 `.agent/workflows/project-index.md` 的小程序运行说明补充“基础库版本不可用”排查与修复指引。

## 增量更新（2026-03-02：小程序 UI/UX 对齐与中文化）

1. 小程序 UI 视觉与 Web 端对齐
- 将小程序整体视觉方向调整为与 Web 核心业务页一致的轻量企业风格（`slate + indigo`）。
- 统一导航栏、主按钮、卡片边框/阴影、状态标签与分区标题的样式层级，降低端间视觉割裂。
- 登录页改为与 Web 登录同系的深色卡片风格，保持品牌一致性。

2. 小程序功能交互增强
- Todo 页新增状态筛选、关键词搜索、行内操作（开始/完成）与加载/空态反馈。
- Project 页新增状态筛选与结构化状态展示。
- Finance 页新增总览统计（余额/收入/支出）与“交易流水/账户列表”切换视图。
- Home 页补充刷新反馈、更新时间显示和高频模块快捷入口优化。

3. 小程序前端字段中文显示
- Tab 文案、页面标题、按钮文案、提示语、空态文案统一改为中文。
- 对状态/方向等字段采用“逻辑值保留英文 + 展示层映射中文”的策略，保证接口兼容与前端可维护性。
- 请求层通用提示（网络异常、登录过期、请求失败）同步中文化。

4. 工作流索引同步
- 已更新 `.agent/workflows/project-index.md`：
  - Frontend 架构中补充小程序“参考 Web 端视觉语言”的约束。
  - Conventions 中补充“小程序用户可见字段默认中文显示”的约束。

## 下一步计划（按优先级）

1. P0：补齐小程序核心操作闭环
- Todo：补 `submit / block / recall` 等动作入口，形成“开始-提交-审核-完成”完整链路。
- Leave：补经理端请假审批动作（通过/驳回）和审批反馈提示。
- Finance：补交易创建最小表单，打通“查看 -> 新增 -> 回刷列表”。

2. P1：强化详情页与跨模块跳转
- Project：新增项目详情页（阶段、成员、任务摘要）并支持从列表进入。
- Contract：补合同详情页基础信息展示，支持从列表项进入。
- Home：指标卡支持点击直达对应筛选后的列表视图。

3. P1：统一交互与视觉规范
- 抽离小程序端通用“状态映射/格式化”工具，减少各页重复逻辑。
- 对齐 Web 端组件语义：按钮层级、空态结构、列表卡片标题/副标题规范。
- 增加关键页面 loading skeleton，减少刷新时闪烁感。

4. P2：质量与发布准备
- 建立关键页面手工验收清单（登录、待办、项目、财务、我的）。
- 补齐常见错误场景覆盖（401、空列表、慢网、字段缺失）。
- 在微信开发者工具完成真机预览与样式回归，固化发布前检查流程。
