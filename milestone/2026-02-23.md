# 2026-02-23 项目进度记录

## 今日工作总结

今天主要围绕 **AI 合同生成模块（AI Contract Generation Canvas）** 进行了完整的功能开发与体验优化。利用大语言模型（Gemini）实现了在“合同签署”阶段自动加载项目上下文并流式起草合法合规的合同正文，同时支持将其导出为标准 Word 文档。

### 1. AI 合同起草工作台 (Contract Canvas Modal)
- **UI/UX 改造**：设计了左右分栏的沉浸式工作台。左侧为 AI 交互与配置面板，右侧为带有 A4 纸张拟真阴影、正规合同排版（宋体、居中标题、首行缩进、带边框表格）的 Markdown 实时预览区。
- **流式对话接口**：后端接入 `Gemini` API 的 `streamGenerateContent` 接口，前端通过 Server-Sent Events (SSE) 实现了打字机效果的实时生成反馈，极大提升了等待体验。
- **多模型无缝切换**：在顶部操作栏加入了模型切换器，支持动态选择模型（默认为稳定的 `Gemini 3.1 Pro`，也可选 `Flash 2.0` 等提速模型）。

### 2. 深度上下文注入 (Project Context Injection)
- **问题设计**：为了让 AI 能够直接输出“可用”的合同，而不仅仅是模板，我们需要将系统内的业务数据喂给 AI。
- **后端接口支持**：新增了 `GET /project/projects/{id}/contract-context` 接口。
- **关联数据穿透**：该接口不仅查询了基础的项目名称和周期，还联表查询了负责人（PM）信息、我方主体（乙方，包含公司名、统一社会信用代码、地址）、客户信息（甲方，包含税号、地址、核心开户行信息）以及关联的合同总金额等核心财务要素。前端在打开 Canvas 时自动加载这些数据，形成隐藏的 `System Prompt` 确保 AI 生成内容的准确率。

### 3. Word 文档导出支持 (Word Export)
- **环境搭建**：在 Conda `punkrecord` 环境中引入了 `python-docx` 库。
- **后端导出接口**：新增了 `POST /project/export-contract-docx` 接口。该接口不仅单纯将 Markdown 转化为文本，更在 Python 层面对段落、通栏表格、加粗列表以及各级标题进行了原生 Word 格式的映射赋值，并且全局设定了标准的 A4 页边距、11pt 宋体字号。
- **前端接入**：右上方增加了一键“导出 Word”按钮，直接将渲染完成的 Markdown 文本流转化为浏览器下载的 `.docx` 文件，满足线下二次编辑和盖章流转的商业需求。

## 待办 / 下一步计划
- 进一步优化 AI Prompt 逻辑，针对特定类型的合同（如 NDA、服务框架协议）设定更垂直的子模板库。
- 将 AI 生成的最终版合同直接对接财务模块的“应收/应付款”计算逻辑。
- 完善合同归档后的 PDF 锁定和水印预览能力。

---

## 下午补充工作

### 4. 从功能清单一键生成开发任务 (Auto-Generate Dev Tasks)

在"开发"阶段新增了批量生成开发任务的完整闭环功能，让项目经理可以直接将需求对齐阶段沉淀的功能清单转化为可追踪的待办任务，大幅减少手工录入。

#### 4.1 后端 API
- **`GET /project/projects/{id}/feature-list`**：从项目阶段中检索功能清单数据（优先匹配 `requirement_alignment` 阶段，并通过键名校验过滤掉报价格式的行项目），同时返回项目成员列表（含 PM）供前端下拉分配。
- **`POST /project/projects/{id}/generate-dev-tasks`**：接收前端配置好的任务列表，批量创建 `TodoItem` 记录，并将 `source_type` 标记为 `PROJECT_TASK`，任务立即出现在被分配人的待办列表中。

#### 4.2 前端组件
- **`GenerateTasksModal.jsx`**：全新的两步弹窗组件。
  - **第一步（选择）**：按模块分组展示功能清单中的每一行，每行按开发类型（后端/前端/UI/产品）拆分为独立任务点，支持单选、模块全选、全选/全不选操作。
  - **第二步（配置）**：以表格形式呈现已勾选条目，支持内联修改任务标题、选择负责人、设定优先级（P0-P3）和截止日期后一键批量提交。
- **`ProjectDetailPage.jsx`**：在"开发"阶段行的操作列新增了 ⚡ **生成开发任务** 按钮（紫色），点击后触发弹窗。
- **`ProjectTasks.jsx`**：升级了项目任务卡片，新增中文状态标签（待开始/进行中/已完成等）、优先级 emoji、开发类型徽章、刷新按钮，以及引导性的空状态提示。

### 5. Bug 修复

| # | 问题 | 原因 | 修复 |
|---|------|------|------|
| 1 | 后端启动失败 `NameError: BaseModel` | 新增的 `DevTaskItem` Pydantic 模型未导入 `BaseModel` | 在 `project.py` 顶部加 `from pydantic import BaseModel` |
| 2 | 合同"保存"按钮无任何反馈 | `handleSave` 是同步一行调用，无 `await`、无错误捕获 | 改为 `async`，加 `try/catch`，按钮显示三态：保存中 / ✅ 已保存 / ❌ 保存失败 |
| 3 | 合同保存返回 404 | 后端 route 为 `PATCH /stages/{id}`，缺少 `projects/{id}/` 前缀，与前端调用路径不一致；`status` 字段为必填 | 修正路由路径，加 `project_id` 所有权校验；将 `status` 改为 `Optional`，`StageStatusUpdate` 新增 `deliverables / feature_list / planned_*` 字段 |
| 4 | 生成任务弹窗内容空白（关键 Bug） | **两个子原因**：① `getDevTypes` 用 `parseFloat > 0` 判断，但工时存为空字符串 / `"-"`，全部被过滤 ② key 格式 `"0_dev_backend"` 用 `split('_')` 拆出 `devType = "dev"` 而非 `"dev_backend"` | ① 改为字符串判断（非空、非 `"-"`、非 `"0"`）② 把分隔符从 `_` 改为 `\|`，用 `indexOf` 精确拆分 |
| 5 | 功能清单数据来源错误（根本原因） | `get_project_feature_list` 优先读取 `quotation`（报价）阶段，该阶段的 `feature_list` 存的是**报价行项目**（含 `total_price` 等），完全不是功能规格 | 将优先级改为 `requirement_alignment → prototype_confirmed → quotation`；新增 `_is_feature_list()` 校验函数，通过键名集合交集判断数据格式是否符合功能清单结构，避免误读 |
