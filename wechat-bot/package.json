{
  "name": "punkrecord-wechat-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Bidirectional WeChat bot for PunkRecord: pushes todo notifications and lets the user approve/reject by quoting a message and replying 通过/拒绝. Brain = Claude (Anthropic SDK) + PunkRecord tools.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "start": "node dist/main.js",
    "test": "npm run build && node --test dist/parse.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "weixin-agent-sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
